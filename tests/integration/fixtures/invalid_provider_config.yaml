# Invalid configuration - invalid model configuration
# Should raise ConfigValidationError before startup

providers:
  gemini:
    api_key: "sk-test-key"
    models:
      flash:
        id: "gemini-2.0-flash-exp"
        max_tokens: -1000  # INVALID: negative max_tokens (must be ≥1)
        temperature: 0.7

teams:
  general:
    description: "General team"
    primary:
      provider: "gemini"
      model: "flash"

tool_team_mapping: {}
