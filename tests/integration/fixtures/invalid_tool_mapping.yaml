# Invalid configuration - tool_team_mapping references non-existent team
# Should raise validation error

providers:
  gemini:
    api_key: "sk-test-key"
    models:
      flash:
        id: "gemini-2.0-flash-exp"
        max_tokens: 8192

teams:
  general:
    description: "General team"
    primary:
      provider: "gemini"
      model: "flash"

tool_team_mapping:
  analyze_code: "analyst"  # INVALID: Team 'analyst' does not exist in teams
  general_query: "general"  # Valid
