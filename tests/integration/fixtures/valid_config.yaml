# Minimal valid SCOUT configuration for testing
# This configuration uses hardcoded API keys (not real) for testing purposes

providers:
  gemini:
    api_key: "sk-test-gemini-key-12345"
    models:
      flash:
        id: "gemini-2.0-flash-exp"
        max_tokens: 8192
        temperature: 0.7
        cost_per_1k_input: 0.0001
        cost_per_1k_output: 0.0003

teams:
  general:
    description: "General-purpose team for basic tasks"
    primary:
      provider: "gemini"
      model: "flash"
    validators: []
    use_cases:
      - "general_queries"
      - "basic_analysis"

tool_team_mapping:
  analyze_code: "general"
  review_document: "general"

system:
  default_team: "general"
  request_timeout_seconds: 60
  max_retries: 3
  cache_ttl_seconds: 3600
  redis_url: null

integrations:
  notion_api_key: null
  tavily_api_key: null
